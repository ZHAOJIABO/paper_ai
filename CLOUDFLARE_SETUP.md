# Cloudflare 完整配置指南

本指南将指导你使用 Cloudflare 方案配置域名访问，包括域名购买、DNS 解析、CDN 和 SSL。

## 📋 准备清单

在开始之前，你需要准备：

- [ ] 一个邮箱账号（用于注册 Cloudflare）
- [ ] 国际信用卡或 PayPal 账号（用于购买域名）
- [ ] 你的 Vultr 日本服务器 IP 地址
- [ ] SSH 访问你的服务器的权限

## 🚀 完整配置流程

### 第一步：注册 Cloudflare 账号

1. 访问 [Cloudflare 官网](https://www.cloudflare.com)
2. 点击右上角 **Sign Up**（注册）
3. 填写邮箱和密码
4. 验证邮箱（查收验证邮件）

**用时：3-5 分钟**

---

### 第二步：购买域名

#### 方式 A：在 Cloudflare 购买（推荐）

1. 登录 Cloudflare 后，点击左侧 **Domain Registration** 或 **注册域名**
2. 在搜索框输入你想要的域名（例如：`mypaperai.com`）
3. 查看可用域名和价格
   - `.com` 约 $9.77/年
   - `.net` 约 $13.07/年
   - `.org` 约 $9.84/年
   - `.ai` 约 $100/年（AI 相关域名，较贵）
4. 选择域名，点击 **Purchase**
5. 填写账单信息
6. 使用信用卡或 PayPal 付款
7. 等待购买完成（通常 1-5 分钟）

**优势：**
- ✅ 自动配置 DNS，无需手动操作
- ✅ 价格透明，无隐藏费用
- ✅ 免费 WHOIS 隐私保护

#### 方式 B：在其他地方购买，转入 Cloudflare

如果你已经在阿里云、NameCheap 等地方购买了域名：

1. 在 Cloudflare 点击 **Add a Site**（添加站点）
2. 输入你的域名
3. 选择 **Free** 计划
4. Cloudflare 会扫描你的 DNS 记录
5. 按照提示修改域名的 **Nameserver（域名服务器）**：
   ```
   更改为 Cloudflare 提供的 Nameserver：
   - ava.ns.cloudflare.com
   - rafe.ns.cloudflare.com
   （具体以 Cloudflare 显示的为准）
   ```
6. 在原域名注册商处修改 Nameserver
7. 等待 DNS 生效（通常 2-24 小时）

**用时：10-15 分钟（不含等待时间）**

---

### 第三步：配置 DNS 解析

1. 在 Cloudflare 控制台，进入你的域名
2. 点击 **DNS** 选项卡
3. 添加 A 记录：

#### 记录 1：根域名
```
Type: A
Name: @
IPv4 address: 你的日本服务器IP（例如：45.76.123.456）
Proxy status: 🟠 Proxied（已代理 - 橙色云朵）
TTL: Auto
```

点击 **Save**

#### 记录 2：www 子域名
```
Type: A
Name: www
IPv4 address: 你的日本服务器IP
Proxy status: 🟠 Proxied（已代理 - 橙色云朵）
TTL: Auto
```

点击 **Save**

**重要说明：**
- 🟠 **橙色云朵（Proxied）** = 启用 CDN 和防护，流量经过 Cloudflare
- ⚪ **灰色云朵（DNS only）** = 仅 DNS 解析，流量直达你的服务器

**推荐使用橙色云朵（Proxied）**，可以获得：
- ✅ 全球 CDN 加速
- ✅ DDoS 攻击防护
- ✅ 自动缓存优化
- ✅ SSL/TLS 加密

**用时：2-3 分钟**

---

### 第四步：配置 SSL/TLS

1. 在 Cloudflare 控制台，点击 **SSL/TLS** 选项卡
2. 选择加密模式：**Full (strict)** （完全严格）

**加密模式说明：**

| 模式 | 说明 | 是否推荐 |
|------|------|----------|
| Off | 不加密 | ❌ 不安全 |
| Flexible | Cloudflare 到用户加密，Cloudflare 到服务器不加密 | ⚠️ 不推荐 |
| Full | 两端都加密，但不验证证书 | ⚠️ 可用 |
| **Full (strict)** | 两端都加密，验证证书 | ✅ **推荐** |

3. 等待几分钟让配置生效

**我们将在服务器上配置 Let's Encrypt 证书，所以选择 Full (strict) 模式。**

**用时：1-2 分钟**

---

### 第五步：配置 Cloudflare 优化设置（可选但推荐）

#### 5.1 开启 Always Use HTTPS

1. 进入 **SSL/TLS** > **Edge Certificates**
2. 找到 **Always Use HTTPS**
3. 开启（ON）

这样所有 HTTP 请求会自动重定向到 HTTPS。

#### 5.2 开启 HSTS

在同一页面：
1. 找到 **HTTP Strict Transport Security (HSTS)**
2. 点击 **Enable HSTS**
3. 配置：
   - Max Age Header: 6 months
   - Apply HSTS policy to subdomains: 关闭（除非你有子域名）
   - No-Sniff Header: 开启
   - 勾选 "I understand"
4. 点击 **Save**

#### 5.3 优化缓存和速度

1. 进入 **Speed** > **Optimization**
2. 开启以下选项：
   - ✅ Auto Minify: JavaScript, CSS, HTML
   - ✅ Brotli 压缩
   - ✅ Rocket Loader（可选，可能影响某些 JS）

3. 进入 **Caching** > **Configuration**
4. Browser Cache TTL: 选择 **4 hours** 或 **Respect Existing Headers**

**用时：5 分钟**

---

### 第六步：在服务器上配置 SSL 证书

现在需要在你的 Vultr 日本服务器上操作。

#### 6.1 连接到服务器

```bash
ssh root@你的服务器IP
```

#### 6.2 进入项目目录

```bash
cd /path/to/paper_ai  # 替换为你的项目路径
```

#### 6.3 上传配置脚本（如果还没有）

如果服务器上还没有 `setup_domain.sh` 脚本，可以通过以下方式上传：

**方式 1：使用 git**
```bash
git pull  # 如果项目在 git 仓库中
```

**方式 2：手动创建**
```bash
# 将本地的 setup_domain.sh 内容复制到服务器
vim setup_domain.sh
# 粘贴脚本内容，保存退出
chmod +x setup_domain.sh
```

**方式 3：使用 scp**
```bash
# 在本地执行
scp setup_domain.sh root@你的服务器IP:/root/paper_ai/
```

#### 6.4 运行配置脚本

```bash
sudo ./setup_domain.sh
```

按照提示：
1. 输入你的域名（例如：`mypaperai.com`）
2. 是否配置 www 子域名：输入 `y`
3. 等待脚本自动完成配置

**脚本会自动完成：**
- ✅ 安装 Certbot
- ✅ 申请 Let's Encrypt SSL 证书
- ✅ 配置 Nginx
- ✅ 设置证书自动续期
- ✅ 重启服务

**如果证书申请失败：**
- 检查 DNS 是否已生效（可能需要等待 5-10 分钟）
- 确认防火墙已开放 80 和 443 端口
- 查看错误信息并根据提示操作

**用时：5-10 分钟**

---

### 第七步：验证配置

#### 7.1 检查 DNS 解析

在本地电脑上执行：

```bash
# 检查域名解析
nslookup your-domain.com

# 或者使用在线工具
# https://www.whatsmydns.net/
```

应该看到你的域名解析到 **Cloudflare 的 IP**（不是你的服务器 IP），这是正常的，因为启用了 CDN。

#### 7.2 检查 SSL 证书

```bash
# 测试 HTTPS
curl -I https://your-domain.com/health
```

应该返回 200 状态码。

#### 7.3 浏览器访问

在浏览器打开：
- `https://your-domain.com`
- `https://www.your-domain.com`

检查：
- ✅ 显示🔒锁图标
- ✅ 证书有效
- ✅ 页面正常加载

#### 7.4 测试 HTTP 重定向

访问 `http://your-domain.com`，应该自动跳转到 `https://your-domain.com`

#### 7.5 检查服务状态

在服务器上执行：

```bash
# 检查容器状态
docker-compose ps

# 查看 Nginx 日志
docker-compose logs nginx --tail=50

# 查看应用日志
docker-compose logs app --tail=50
```

**用时：5 分钟**

---

## 🎉 完成！

如果所有验证都通过，恭喜你已经成功配置了：
- ✅ 域名解析
- ✅ HTTPS 加密
- ✅ Cloudflare CDN 加速
- ✅ DDoS 防护
- ✅ 自动证书续期

你的 Paper AI 服务现在可以通过域名访问了！

---

## 📊 性能优化建议

### 配置 Cloudflare 缓存规则

1. 进入 Cloudflare 控制台 > **Rules** > **Page Rules**
2. 创建规则缓存静态资源（如果有的话）

例如：
```
URL: your-domain.com/static/*
Settings: Cache Level = Cache Everything
```

### 配置 Firewall 规则

1. 进入 **Security** > **WAF**
2. 开启 **Managed Ruleset**
3. 根据需要配置自定义规则

---

## ⚠️ 常见问题

### Q1: DNS 解析显示 Cloudflare IP 而不是我的服务器 IP

**答：** 这是正常的！因为你启用了 Cloudflare CDN（橙色云朵），流量会先到 Cloudflare，然后转发到你的服务器。

### Q2: SSL 证书申请失败

**解决方案：**

1. **检查 DNS 是否生效**
   ```bash
   nslookup your-domain.com
   ```

2. **临时关闭 Cloudflare 代理**
   - 在 Cloudflare DNS 设置中，将橙色云朵点击变为灰色
   - 等待 2-3 分钟
   - 重新运行 `setup_domain.sh`
   - 证书申请成功后，重新开启橙色云朵

3. **检查防火墙**
   ```bash
   sudo ufw status
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   ```

### Q3: 网站可以访问但很慢

**解决方案：**

1. 确认 Cloudflare CDN 已开启（橙色云朵）
2. 检查 Cloudflare 缓存设置
3. 在 Cloudflare 中开启 **Auto Minify** 和 **Brotli**

### Q4: Cloudflare 显示 522 错误

**错误含义：** Cloudflare 无法连接到你的源服务器。

**解决方案：**

1. 检查服务器是否运行：
   ```bash
   docker-compose ps
   ```

2. 检查 Nginx 是否正常：
   ```bash
   docker-compose logs nginx
   ```

3. 确认服务器防火墙允许 Cloudflare IP 访问：
   - Cloudflare IP 列表：https://www.cloudflare.com/ips/

### Q5: 中国大陆访问慢或无法访问

**说明：** Cloudflare 在中国大陆没有数据中心，访问可能受限。

**解决方案：**

1. **使用 Cloudflare Partners（中国网络）**
   - 某些 CDN 服务商与 Cloudflare 合作，在中国有节点

2. **双线路方案**
   - 境外用户：Cloudflare
   - 中国大陆用户：阿里云 CDN
   - 使用 DNS 智能解析分流

3. **考虑添加中国香港节点**
   - 在 Vultr 或其他平台部署香港节点
   - 距离中国大陆更近

---

## 📝 维护清单

### 每月检查

- [ ] 查看 SSL 证书有效期（自动续期应该会处理）
- [ ] 查看 Cloudflare 流量统计
- [ ] 检查服务器负载和性能

### 证书续期

Let's Encrypt 证书有效期 90 天，但我们已配置自动续期。如需手动续期：

```bash
sudo certbot renew
cd /path/to/paper_ai
sudo cp /etc/letsencrypt/live/your-domain.com/fullchain.pem ssl/
sudo cp /etc/letsencrypt/live/your-domain.com/privkey.pem ssl/
docker-compose restart nginx
```

### 查看 Cloudflare 分析

在 Cloudflare 控制台可以看到：
- 流量统计
- 缓存命中率
- 威胁阻止数量
- 性能指标

---

## 🔗 有用的链接

- [Cloudflare 官方文档](https://developers.cloudflare.com/)
- [Cloudflare 状态页面](https://www.cloudflarestatus.com/)
- [Let's Encrypt 文档](https://letsencrypt.org/docs/)
- [SSL Labs 测试工具](https://www.ssllabs.com/ssltest/)

---

## 💰 费用估算

使用 Cloudflare 方案的年度费用：

| 项目 | 费用 |
|------|------|
| 域名（.com） | ~$10/年 |
| Cloudflare 计划 | $0/年（免费） |
| Let's Encrypt 证书 | $0/年（免费） |
| Vultr 服务器 | $5-10/月 = $60-120/年 |
| **总计** | **~$70-130/年** |

相比其他方案，这已经是最经济实惠的配置了！

---

## 📞 获取帮助

如果遇到问题：

1. **查看日志**
   ```bash
   docker-compose logs
   ```

2. **检查服务状态**
   ```bash
   docker-compose ps
   systemctl status docker
   ```

3. **Cloudflare 社区**
   - https://community.cloudflare.com/

4. **Let's Encrypt 社区**
   - https://community.letsencrypt.org/

祝配置顺利！🎉
